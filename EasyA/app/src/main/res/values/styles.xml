<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item> <!-- app name bar color -->
        <item name="colorPrimaryDark">@android:color/white</item> <!-- top bar color -->
        <item name="colorAccent">@color/colorAccent</item> <!-- ???? color -->
    </style>

</resources>
